<div class="container">
  <div class="row">
      <% @courses.each do |course| %>
      <% if course.user_id == current_user.id %>
      <div class="col-xs-12 col-sm-12 col-md-6">
        <div class="card">
          <div class="front">
            <img src="http://res.cloudinary.com/arthurpfister/image/upload/v1527849942/goran-ivos-343495-unsplash.jpg" alt="" class="center">
            <div class="caption">
              <h2><%=course.title %></h2>
              <h6><%=course.price%>€    per hour</h6><br>
              <p><%= course.description[0,300] %>...
                <a href="#"><%= link_to "", course_path(course)%></a>
              </div>
            </div>
          </div>
        </div>
        <% end %>
        <% end %>

    </div>
  </div>
