var new_list_html = '<%= j render "list" %>'
var new_map_html = '<%= j render "map" %>'

  var newList = document.getElementById('courses-list');
  newList.innerHTML = new_list_html;

  var newMap = document.getElementById('courses-map');
  newMap.innerHTML = new_map_html;

  document.loadGoogleMap();



