const new_list_html = '<%= j render "list" %>'
const new_map_html = '<%= j render "map" %>'

  const newList = document.getElementById('courses-list');
  newList.innerHTML = new_list_html;

  const newMap = document.getElementById('courses-map');
  newMap.innerHTML = new_map_html;



